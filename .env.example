# Copy to .env.local (never commit real values)
# Next.js env vars:
# - Use NEXT_PUBLIC_* for values that are safe to expose to the browser.
# - Everything else stays server-only.

# Example analytics (optional)
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=digitalhooligan.io

# Example app config (optional)
# NEXT_PUBLIC_SITE_URL=https://digitalhooligan.io

# Example server-only secrets (optional)
# API_KEY=
