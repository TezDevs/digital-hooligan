// apps/digitalhooligan-web/app/ceo/login/page.tsx
// (adjust path to match what grep showed)

"use client";

import Link from "next/link";

export default function CeoLoginPage() {
return (
<main className="flex min-h-screen items-center justify-center bg-slate-950 text-slate-50">
    <div className="w-full max-w-md rounded-3xl border border-slate-800 bg-black/70 p-6 shadow-xl shadow-black/60">
        <h1 className="text-lg font-semibold text-slate-50">
            CEO dashboard access
        </h1>
        <p className="mt-1 text-sm text-slate-400">
            Hidden grown-up entrance for the Digital Hooligan command center.
        </p>

        <div className="mt-5 space-y-4">
            <div>
                <label
                    className="mb-2 flex items-center gap-2 text-xs font-medium uppercase tracking-[0.2em] text-slate-400">
                    <span className="text-sm">🛡️</span>
                    Access key
                </label>
                <input type="password" placeholder="For now, anything goes. Later, real auth."
                    className="w-full rounded-xl border border-slate-700 bg-black/60 px-3 py-2 text-sm text-slate-50 outline-none ring-0 placeholder:text-slate-500 focus:border-emerald-500 focus:ring-1 focus:ring-emerald-500" />
                <p className="mt-1 text-[0.7rem] text-amber-300">
                    Enter anything for now – this is a soft gate.
                </p>
            </div>

            {/* DEV: pure link, no auth */}
            <Link href="/ceo"
                className="flex w-full items-center justify-center rounded-full bg-emerald-500 px-4 py-2 text-sm font-semibold text-black shadow-lg shadow-emerald-500/30 transition hover:bg-emerald-400">
            Enter CEO dashboard →
            </Link>
        </div>

        <div className="mt-4 flex items-center justify-between gap-2 text-[0.75rem] text-slate-400">
            <Link href="/"
                className="rounded-full border border-slate-700 px-3 py-1.5 text-xs font-medium text-slate-200 hover:border-slate-500 hover:text-slate-50">
            Back to marketing site
            </Link>
            <span className="text-[0.7rem] text-slate-500">
                Future: wire into real auth &amp; audit.
            </span>
        </div>
    </div>
</main>
);
}